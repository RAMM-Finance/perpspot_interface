export enum TokenStatus {
    HIGHER = 'New',
    BUILD = 'New',
    KEYCAT = 'New',
    SPEC = '🔥',
    OKAYEG = '🔥',
    CHOMP = 'New',
    PRIME = 'New'
  }

export type TokenStatusKey = keyof typeof TokenStatus